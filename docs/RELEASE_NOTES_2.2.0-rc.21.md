# Worldcraft Codex 2.2.0-rc.21

`2.2.0-rc.21` 是面向 2026-07-31 现场展演的 Windows x64 候选版，重点是演示可靠性、界面收口、正式案例完整性和安全分发边界。

## 本版变化

- 世界切换器直接显示每个世界的条目、地图和章节数量，大型案例更容易辨认。
- 小说书稿在桌面与窄窗口中采用固定工作台，页面不再与正文编辑区同时出现纵向滚动条。
- 资源库消除外层页面的多余滚动，只保留资源详情面板自身的滚动区。
- 新增只读 `demo:check`，核验 SQLite、schema、两个主案例规模、山海经物理资源和外部链接边界。
- 新增 `demo:open`，预检通过后再启动桌面软件。
- 自动录屏脚本必须使用独立数据目录，并拒绝正式用户目录或异常庞大的演示数据库。
- 主题与 Wiki 的 Electron 回归默认加载生产构建，不再依赖本地开发服务器。
- Next.js 升级到 `16.2.12`，并锁定已修复的 PostCSS 与 sharp 版本；生产依赖审计为 0 个已知漏洞。
- 升级安装测试会等待应用文件与卸载注册项都完成清理，消除 NSIS 异步收尾造成的偶发误报。
- 补充展演路线、故障备用流程、GitHub 首页、提交排除规则与签名公开版手动工作流。

## 案例核验

- `中国上古神话史`：2,442 个条目、7,333 条关系、635 个历史节点、424 条来源记录。
- `山海经 · 原典内容全集重制版`：1,909 个条目、21 张地图、284 个资源、387 条关系、18 个章节、8 条任务线。
- 山海经严格完整性审计全部通过：今译、复核状态、插图目录、独立资源、子地图底图、数据库关联和书稿结构均为 0 个缺口。
- 山海经重制版 284/284 个物理资源可用，外部链接与第三方 Wiki 痕迹均为 0。

## 验证结果

- 43 阶段完整发布门禁通过，生产依赖漏洞为 0。
- Electron 桌面回归通过：529 项断言，覆盖正常退出、正常重启与异常退出恢复。
- 五套主题通过生产构建回归；夜间主题正文对比度为 15.51:1。
- Wiki Electron 回归通过：2,201 个公开可见条目，测试搜索耗时 102 ms。
- 10,000 条目性能基准通过；冷加载 96 ms，FTS 搜索 1 ms。
- 打包后 `.wcodex` 往返通过：31 项断言、3 次真实打包应用启动。
- NSIS 干净安装与卸载通过：12 项断言。
- Portable 冷启动通过：12 项断言，SQLite `quick_check=ok`，schema 17，首次启动约 10.9 秒。
- `2.2.0-rc.20 → 2.2.0-rc.21` 覆盖升级通过：25 项断言，世界、条目、数据库与备份均保留。
- 产物完整性验证通过 46 项断言，打包安全验证通过 27 项断言。

## Windows 产物

- `Worldcraft Codex-Setup-2.2.0-rc.21.exe`：128.83 MiB
  SHA-256 `283f3357998ba1ae548859ea7174d9bbf68680e6d34a9c00206430159027150e`
- `Worldcraft Codex-Portable-2.2.0-rc.21.exe`：128.66 MiB
  SHA-256 `b3da4154705bd61e957403c772e81171ae935c41f003d3f53759145cc3b47825`

Release 同时附带 `SHA256SUMS.txt`、CycloneDX SBOM、构建来源证明、产物清单和第三方依赖清单。

## 分发说明

- 本候选包未配置 Authenticode 代码签名，Windows 可能显示未知发布者提示。
- 当前适合私有评审、现场展演和受控测试，不应描述为已签名正式稳定版。
- 正式公开稳定版仍需要法律主体、隐私/条款/支持地址、更新地址和可信代码签名证书。
- 用户数据库、备份、API Key、日志、缓存与本机诊断不会进入 Git 仓库或发行包。
- 当前产品范围不包含社区、实时多人协作和插件市场。
